console.log('Hello')
let name2:string = 'tejas'

let arr: number[] = [1,2,3]; // if we need to store numbers inside this array
let arr2: string[]= ['dog','cat','rabbit'] // if we need to store strings inside an array

//another way of defining arrays=>
let arr3: Array<number> = [1,2,3,4] // Generic functions method of defining arrays

let bag:string='levis'

// Objects=>
let obj:{id:number; name: string}= {
    id: 2,
    name: 'tejas'
} // in objects we need to define the type of each and every key
